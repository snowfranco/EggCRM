{
  "name": "novacrm-demo-build",
  "private": true,
  "version": "0.1.0",
  "description": "Builds the EggCRM demo UI (localdoc/novacrm-demo.jsx) into webui/app.js. Deps live at the repo root so the component (localdoc/) and entry (webui/) resolve a SINGLE React copy.",
  "scripts": {
    "build": "esbuild webui/main.jsx --bundle --outfile=webui/app.js --loader:.jsx=jsx --jsx=automatic --minify --define:process.env.NODE_ENV='\"production\"'"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
